{
    "name": "docker-hub-proxy",
    "version": "1.0.1",
    "description": "Cloudflare Worker as Docker Hub pull-through proxy",
    "private": true,
    "scripts": {
        "deploy": "wrangler deploy"
    },
    "devDependencies": {
        "wrangler": "^4.113.0"
    }
}
